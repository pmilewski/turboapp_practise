<div id="<%= dom_id list %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <strong class="block font-medium mb-1">List:</strong>
    <%= list.name %>
  </div>
  <div class="border p-5 m-5">
    <% list.tasks.each do |task| %>
      <%= render partial: "lists/task", locals: { task: task } %>
    <% end %>
  </div>
</div>
